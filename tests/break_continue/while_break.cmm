// while_break: while(1) + break 构成条件退出
int main() {
    int sum = 0;
    while (1) {
        if (sum > 10) {
            break;
        }
        sum = sum + 3;
    }
    return sum;    // 3,6,9,12 → 12
}
